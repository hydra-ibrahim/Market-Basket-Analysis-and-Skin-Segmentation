// Single source of truth for the backend URL — every page loads this file first.
// Update this one line once your backend is deployed, e.g. 'https://your-app.onrender.com'.
const API_BASE_URL = 'http://127.0.0.1:8000';
